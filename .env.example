# Example environment file. Copy to `.env` and fill in real values.
# `.env` is git-ignored and must never be committed.
#
# No secrets are required yet. This file documents the convention so that
# secrets are kept out of source control from day one.
#
# Example (placeholder, not currently used):
# QUANTFORGE_DATA_DIR=./data
