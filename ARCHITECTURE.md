# Architecture

This document describes the **intended** high-level architecture of QuantForge
and the **current status** of each component.

> **Important:** The acquisition, storage, parsing, provenance, point-in-time,
> derived-metrics, factor, universe, panel, market-data, backtesting,
> comparative-research, research-reporting, performance-analytics,
> cross-sectional signal-diagnostics, multi-factor performance-attribution,
> cross-sectional factor-return-regression (Fama–MacBeth premia),
> characteristic-sorted long/short factor-portfolio-construction,
> factor-risk-model (factor covariance & correlation estimation),
> factor-risk-aware portfolio-optimization (fully-invested global
> minimum-variance), walk-forward out-of-sample evaluation, and out-of-sample
> research-campaign evaluation with selection-bias correction (Probabilistic &
> Deflated Sharpe Ratios), and pairwise out-of-sample strategy comparison
> (paired-difference t-tests over date-aligned OOS return series)
> layers now exist (Phases 1–24; see "Implemented layers" below).

> **Data model.** The canonical financial-fact and provenance model that
> underpins the immutable-raw-data, normalization, provenance, and
> point-in-time components is specified in
> [docs/data-model.md](docs/data-model.md). Point-in-time eligibility is gated
> by a *derived, policy-versioned* public-availability timestamp with an
> explicit `verified`/`derived`/`unknown` status — acceptance alone never
> proves availability, and un-datable facts fail closed (are excluded), never
> guessed. It is design-only; no storage or query code exists yet.

> **Implemented layers.** Five deterministic, provenance-first layers now
> exist, each documented in its own spec: **Phase 1 — SEC acquisition**
> ([docs/sec-acquisition.md](docs/sec-acquisition.md)), the immutable
> content-addressed raw-artifact store; **Phase 2 — filing registry**
> ([docs/filing-registry.md](docs/filing-registry.md)), derived filing identity
> and provenance; **Phase 3 — raw XBRL ingestion**
> ([docs/xbrl-ingestion.md](docs/xbrl-ingestion.md)), immutable loss-preserving
> `RawFact`/`RawContext`/`RawUnit` records parsed from the acquired XBRL
> instances; **Phase 4 — canonicalization**
> ([docs/canonicalization.md](docs/canonicalization.md)), deterministic,
> structured canonical `Fact` observations derived from the raw facts with
> complete lineage back to the source; and **Phase 5 — public availability &
> point-in-time** ([docs/point-in-time.md](docs/point-in-time.md)), a versioned,
> fail-closed derivation of when each filing became public plus point-in-time
> (PIT) and revised knowledge-state queries served as distinct, impossible-to-
> confuse result types over the immutable canonical facts. A thin **company
> identity & public API** layer
> ([docs/company-api.md](docs/company-api.md)) sits above these: it resolves a
> ticker, CIK, or company name to the canonical filer identity via SEC's
> official mapping (cached as a Phase 1 artifact) and exposes
> `from quantforge import Company` as the front door, delegating `filings()`
> and `facts()` to the registry and canonical layers without duplicating them.
> Above them, a **financial metrics & research layer**
> ([docs/metrics.md](docs/metrics.md)) computes deterministic, fail-closed,
> versioned, fully-provenanced derived metrics (ratios and arithmetic
> combinations) on demand over the point-in-time knowledge state — served, like
> Phase 5, as distinct PIT and revised result types so a metric can never
> silently consume revised history. Above the market-data layer, a **backtesting
> / research-simulation layer** ([docs/phase12-backtesting-proposal.md](docs/phase12-backtesting-proposal.md))
> now exists: a deterministic, point-in-time strategy simulator over the pinned
> fundamentals + market corpora, content-addressed end to end.

## High-level data flow

```
        Public Financial Data
                 |
                 v
            Ingestion
                 |
                 v
        Immutable Raw Data
                 |
                 v
      Parsing / Normalization
                 |
                 v
            Provenance
                 |
                 v
       Point-in-Time Data Layer
                 |
                 v
              Factors
                 |
                 v
            Backtesting
                 |
                 v
        Reproducible Research
```

## Components and status

| Component | Status | Description |
| --- | --- | --- |
| **Public Financial Data** | 🔜 Planned | Public sources (e.g. regulatory filings, public market data). External inputs; no source connectors exist yet. |
| **Ingestion** | 🔜 Planned | Fetches raw data from public sources and records exactly what was retrieved and when. |
| **Immutable Raw Data** | 🔜 Planned | Append-only store of raw source data, never modified after capture. The system of record. |
| **Parsing / Normalization** | 🔜 Planned | Deterministic transformation of raw data into normalized structures, derived only from immutable raw data. |
| **Provenance** | 🔜 Planned | Every normalized/derived value is traceable to the raw record and process that produced it. |
| **Point-in-Time Data Layer** | ✅ Exists (Phase 5) | Serves data *as it was known* at a given date, preventing look-ahead bias. Derives each filing's public-availability timestamp under a versioned, fail-closed policy and answers PIT / revised queries as distinct result types. See [docs/point-in-time.md](docs/point-in-time.md). |
| **Company identity & public API** | ✅ Exists | The `from quantforge import Company` front door: resolves ticker/CIK/name to the canonical filer identity via SEC's official mapping (cached as a Phase 1 artifact), then delegates `filings()`/`facts()` to the registry and canonical layers. Adds no data model or storage of its own. See [docs/company-api.md](docs/company-api.md). |
| **Factors** | ✅ Exists (Phase 7) | Computed signals/features built strictly on point-in-time data. The metrics layer computes deterministic, fail-closed, versioned, fully-provenanced financial metrics (ratios and arithmetic combinations) on demand over the Phase 5 knowledge state, served as distinct PIT / revised result types. See [docs/metrics.md](docs/metrics.md). |
| **Universe management** | ✅ Exists (Phase 9.1) | A deterministic, immutable, point-in-time collection of filers, assembled from tickers/CIKs/names via `Universe.from_companies([...])`. Resolves through the existing company identity layer (no new identifier system), preserves first-seen ordering and per-member provenance, and content-addresses the ordered membership. The foundation for cross-sectional research; ranking, portfolios, and backtesting are not implemented here. See [docs/universe.md](docs/universe.md). |
| **Universe construction** | ✅ Exists (Phase 9.2) | A deterministic construction framework on top of Phase 9.1: `UniverseSpecification` (an immutable, content-addressed, ordered list of selection rules) → `UniverseBuilder` (a fail-closed engine that evaluates it at one PIT/REVISED boundary) → a `Universe` plus a reproducible `UniverseConstruction` provenance record. The three initial filters (`ExplicitCompanyFilter`, `CompanyMetricFilter`, `SectorFilter`) *compose* the existing resolver and metric engine — no new identifier system, no arithmetic, no external I/O. Same specification + same builder + same data ⇒ same `universe_id` and `construction_id`. See [docs/universe-construction.md](docs/universe-construction.md). |
| **Universe research surface** | ✅ Exists (Phase 9) | The researcher-facing completion of Phase 9, on the *same* `Universe` object (no second abstraction): inspection (`members()`, `company_ids`, `contains()`), deterministic serializable description (`describe()` → `UniverseSummary`), membership comparison by canonical `company_id` (`compare()` → `UniverseComparison`, which flags any PIT/REVISED `mode_mismatch`), and dependency-free export (`to_dict()`, `to_records()`). Computes no financial statistic and adds no dependency. `ConstructionResult` exposes the same surface enriched with construction provenance. See [docs/phase9-research-layer.md](docs/phase9-research-layer.md). |
| **Fundamental panel** | ✅ Exists (Phase 10) | One Phase 7 metric evaluated over an explicit, content-addressed period axis, in three shapes — period-series, vintage/knowledge-evolution, and cross-sectional matrix (the last reusing the Phase 9 `Universe`) — with `UNDEFINED`-preserving multi-period derivations (`growth`, `ttm`, `average_balance`, `level_vs_history`). Compute-on-demand and fully provenanced per cell, served as distinct `PitPanel` / `RevisedPanel` types (a `RevisedPanel` can never be passed where a `PitPanel` is required). Adds no market data and no backtester. See [docs/panel.md](docs/panel.md); the [locked architecture](docs/phase10-panel-locked.md) is the normative spec. |
| **Market data** | ✅ Exists (Phase 11) | A provider-neutral, point-in-time market-data layer added as a *new source beneath* the existing stack (never through it). Canonical **unadjusted** daily OHLCV `PriceObservation`s keyed by `(security_id, trading_date, field)`, plus first-class immutable `CorporateAction`s (split / dividend / symbol-change / delisting / merger), each carrying its own fail-closed availability boundary under a versioned market `AvailabilityPolicy`. Instrument identity reuses the designed `security_id` (`cik:<CIK>#class:<...>`; ticker is never identity). Adjusted prices are a derived, versioned, PIT-gated *view* over the immutable actions — never the stored value. Served as distinct `PitPrice` / `RevisedPrice` types (a `RevisedPrice` can never be passed where a `PitPrice` is required). Reuses the Phase 1 content-addressed storage and Phase 5 PIT machinery; edits no prior store, adds no runtime dependency, introduces no database, and builds no backtester. Never rewrites a SEC `Fact`. See the [locked architecture](docs/phase11-market-data-locked.md). |
| **Backtesting** | ✅ Exists (Phase 12) | A deterministic, point-in-time strategy simulator over the pinned fundamentals + market corpora. A strategy is a declarative, content-addressed spec (`signal → rank → select → weight`); the engine owns execution (strategies emit only `TargetWeights`), consuming the Phase 11 PIT-only hand-off (`price_as_of` / `price_series_as_of` returning `PitPrice` / `PitPriceSeries`), applying `CorporateAction`s (split / dividend / delisting / merger / symbol-change) through Phase 11, honoring survivorship through Phase 9, and computing v1 statistics (cumulative/period return, Sharpe, max drawdown, turnover). Every decision at time `T` sees only PIT-eligible-at-`T` data (BT-2); **both** corpus snapshots are pinned and verified (BT-1); missing data is recorded in the ledger, never guessed (BT-4); and the whole run is content-addressed by a `backtest_id` folding every result-changing input (D6) — same inputs reproduce the same id and result on any machine. Reuses the existing stores and PIT machinery; adds no runtime dependency and no database. See [docs/phase12-backtesting-proposal.md](docs/phase12-backtesting-proposal.md). |
| **Reproducible Research** | ✅ Exists (Phase 13) | Comparative research as one capability strictly *above* Phase 12, a pure consumer of already-sealed, PIT-correct backtests. A declarative, content-addressed `ExperimentSpecification` sweeps a closed v1 vocabulary of backtest parameters (D7) — corpus pins are inherited verbatim, never swept (D2) — and `ExperimentEngine.run` deterministically expands the Cartesian product, runs each child through the Phase 12 engine (threading the annualization convention unchanged, D5), and seals a thin `ExperimentResult` ledger of `(coordinate, backtest_id)` pointers to the shared research sidecar (write-once, no new store, D4). `BacktestComparison` ranks a set of sealed backtests (or an experiment's children) by one v1 performance statistic: fail-closed on unknown statistics/orders, absent members, and incommensurable engine versions; corpus `pin_mismatch` is surfaced, never silently compared. `experiment_id` folds every result-changing input, so an identical experiment reproduces the same id, children, and result on any machine — reuse is by determinism + idempotent write-once, never a pre-run guess. Adds no runtime dependency, no database, and no arbitrary-code escape hatch. See the [locked architecture](docs/phase13-comparative-research-locked.md). |
| **Research reporting & explainability** | ✅ Exists (Phase 14) | A reporting layer strictly *above* Phase 13, a pure consumer of already-sealed, PIT-correct artifacts. A declarative, content-addressed `ReportSpecification` (a name, a closed-vocabulary `scope` of `{backtest, experiment}`, the `subject_id` being reported, and optional `ComparisonDirective`s ranking an experiment's children by a Phase 13 statistic — D7) drives `ReportEngine.build`, which resolves and verifies each referenced artifact from the shared sidecar (fail closed on any missing/drifted reference — G7) and seals a `ResearchReport`: a **reference-only** manifest of `(kind, reference_id, content_hash, detail)` pointers (never a copy of any financial value — D3) plus the reporting intent and an explicit `boundary_kind = "pit"` (PIT-only v1 — D10). The report is a `ResearchRecord` persisted write-once to the same Phase 8 sidecar (no new store — D1), byte-identically round-tripping (D4). `report_id` folds the request + referenced content hashes only (D2) — never presentation, schema/format version (D9), or time — so a report's identity is sensitive to any change in any artifact it reports on yet invariant under renderer/heading edits. A comparison is referenced by intent and recomputed deterministically at build/render (never persisted, `analysis.py` untouched — D5). Exactly one pure `render_markdown(report, store)` proves the content/presentation split (D6): it resolves references and formats the ten documented sections without any feedback into identity or storage. Adds no runtime dependency, no database, and edits no existing record's identity. See the [locked architecture](docs/phase14-reporting-locked.md). |
| **Performance & benchmark-relative analytics** | ✅ Exists (Phase 15) | A risk & benchmark-relative analytics layer strictly *above* Phase 12, a pure consumer of already-sealed, PIT-correct `BacktestResult`s that computes the statistic family the Phase 12 `stats.py` docstring explicitly deferred. A declarative, content-addressed `AnalyticsSpecification` (a name, a `subject_id`, an optional `benchmark_id`, the VaR confidences, and the annualization convention `risk_free_per_period` / `periods_per_year` — folded into identity, D8) drives `AnalyticsEngine.compute`, which resolves and re-verifies each referenced backtest's `result_hash` from the shared sidecar (fail closed on any absent/drifted reference), computes the **absolute** block (downside deviation, Sortino, Calmar, max-drawdown duration & recovery, skewness, excess kurtosis, best/worst period, positive-period fraction), the **historical nearest-rank** VaR/CVaR per requested confidence (deterministic, no RNG, no distribution assumption — D7), and — only against a **benchmark that is itself a sealed backtest** (D3) — the **relative** block (active & cumulative-active return, tracking error, information ratio, correlation, up/down capture, single-factor closed-form OLS alpha/beta — D4; multi-factor deferred). Return / volatility / Sharpe / max-drawdown are **not** recomputed — Phase 15 adds only what Phase 12 does not already seal (D2). Every undefinable statistic (zero downside, zero variance, zero benchmark variance, zero tracking error, no drawdown, unrecovered drawdown, insufficient periods) is a first-class `UNDEFINED` cell carrying its reason — never fabricated, never a divide-by-zero, never `NaN`/`Inf` (D5). Subject and benchmark must share `schedule_id`, equal return length, and a commensurable engine version and PIT boundary or the run fails closed; corpus `pin_mismatch` is surfaced, never silently compared. The sealed `PerformanceAnalytics` is a `ResearchRecord` persisted write-once to the same Phase 8 sidecar (no new store — D1), byte-identically round-tripping; `analytics_id` folds the engine+formula version, the declared request, both referenced content hashes, and the computed answer (one id — D6). All arithmetic runs under the pinned `Decimal` context (prec 34, `ROUND_HALF_EVEN`); no float, wall-clock, or RNG enters any value or id. Adds no runtime dependency, no database, no new PIT resolution, and edits no existing record's identity. See the [locked architecture](docs/phase15-analytics-locked.md). |
| **Cross-sectional signal diagnostics** | ✅ Exists (Phase 16) | A cross-sectional signal-diagnostics layer *parallel* to Phase 12 — the diagnostic sibling of the backtester — sitting strictly *above* the Phase 9 universe, Phase 10 panel, and Phase 11 price layers and composing them only (it never consumes a `BacktestResult`). A declarative, content-addressed `SignalDiagnosticsSpecification` (a name, one signal `metric_key`, the explicit `MetricPeriod` it is read for, a Phase 9 `UniverseSpecification`, a Phase 12 `RebalanceSchedule` of evaluation `as_of` instants, a `"<n>d"` forward horizon, a quantile count, the closed IC-method set, and the two corpus pins — all folded into identity) drives `SignalDiagnosticsEngine.evaluate`, which re-verifies **both** corpora (fundamentals `dataset_version_id` **and** market `market_dataset_version_id`) and fails closed on any pin mismatch (SD-1 — a changed corpus yields a different `diagnostics_id`). At each scheduled `T` it reads the signal as a **PIT-eligible-at-`T`** value (SD-3) and pairs it against the realized **forward** return over the horizon; a member lacking a PIT signal at `T` or a computable forward return is *excluded and recorded in coverage, never imputed* (SD-4, fail-closed pairing with auditable coverage). It computes per-date **Spearman** and **Pearson** IC of the signal against forward returns, quantile-bucket profiles + a top-minus-bottom spread, and an IC summary (mean, population std, information ratio, t-stat, hit-rate). The forward return is a diagnostic, **not** a PIT value — there is no `Pit*` type and no as-of accessor for it; `boundary_kind="pit"` documents only the signal side (SD-2). Every undefinable statistic (insufficient pairs, zero signal / return variance, empty bucket, no valid dates) is a first-class `UNDEFINED` cell carrying its reason — never a fabricated `0`, `NaN`/`Inf`, or divide-by-zero. All arithmetic runs under the pinned `Decimal` context (prec 34, `ROUND_HALF_EVEN`); no float, wall-clock, or RNG enters any value or id. The sealed `SignalDiagnostics` is a `ResearchRecord` persisted write-once to the same Phase 8 sidecar (no new store), byte-identically round-tripping; `diagnostics_id` folds the engine + formula + spec version, the full declared request, **both** corpus pins, and the `result_hash` over the computed answer. Adds no runtime dependency, no database, and no new PIT resolution. See the [locked architecture](docs/phase16-signal-diagnostics-locked.md). |
| **Multi-factor performance attribution** | ✅ Exists (Phase 17) | A multi-factor attribution layer strictly *above* Phase 12 — a sibling of the Phase 15 analytics layer and the multi-factor generalization Phase 15 explicitly deferred — a pure consumer of already-sealed, PIT-correct `BacktestResult`s. A declarative, content-addressed `AttributionSpecification` (a name, one `subject_id`, an **ordered** tuple of at most `K_MAX = 8` factor `backtest_id`s — each a sealed backtest, the Phase 15 D3 convention generalized from one benchmark to *K* factors, D1 — and the annualization convention `risk_free_per_period` / `periods_per_year`, folded into identity) drives `AttributionEngine.attribute`, which resolves and re-verifies each referenced backtest's `result_hash` from the shared sidecar (fail closed on any absent/drifted reference), enforces commensurability (same `schedule_id`, equal `period_returns` length, same `backtest_engine_version_id` — FA-3, fail closed) and sufficient degrees of freedom (`n >= K + 2`), and regresses the subject's **excess** return on the *K* factor **excess** returns (excess-on-excess) via an exact-`Decimal` LDLᵀ normal-equation solve with an **exact zero-pivot test**. It reports per-factor betas + alpha, R² / adjusted R² / residual standard error, the classical `sigma_sq·(XᵀX)⁻¹` coefficient standard errors / t-statistics (D5), and a sample mean-excess-return decomposition. Every undefinable statistic (a singular/collinear design, a zero-variance regressand, a perfect fit) is a first-class `UNDEFINED` cell carrying its reason — never a fabricated coefficient, silently dropped factor, `NaN`/`Inf`, or divide-by-zero (FA-4). The output is **ex-post, not PIT** (FA-2): `FactorAttribution` is not a `Pit*` type and exposes no as-of accessor; `boundary_kind="pit"` documents only that the underlying backtests were PIT walks. Distinct corpus pins across references are surfaced as `pin_mismatch`, never silently reconciled (FA-1). The sealed `FactorAttribution` is a `ResearchRecord` persisted write-once to the same Phase 8 sidecar (no new store), byte-identically round-tripping; `attribution_id` folds the engine + formula version, the full declared request, the subject and every ordered factor `result_hash`, and the `result_hash` over the computed answer (one id, D2). Only a deterministic residual **digest** is persisted, never the residual series (D4). All arithmetic runs under the pinned `Decimal` context (prec 34, `ROUND_HALF_EVEN`); no float, wall-clock, or RNG enters any value or id. Adds no runtime dependency, no database, and no new PIT resolution. See the [locked architecture](docs/phase17-factor-attribution-locked.md). |
| **Cross-sectional factor-return regression** | ✅ Exists (Phase 18) | A cross-sectional factor-return-regression layer (the Fama–MacBeth method) — the multivariate cross-sectional sibling of the Phase 16 signal-diagnostics layer (as Phase 17 is the multivariate time-series sibling of Phase 15) — sitting strictly *above* the Phase 9 universe, Phase 10 panel, and Phase 11 price layers and composing them only (it never consumes a `BacktestResult`). A declarative, content-addressed `CrossSectionalRegressionSpecification` (a name, an **ordered** tuple of at most `K_MAX = 8` `FactorSpec` signals — each a `(metric_key, MetricPeriod)`, order semantic and never sorted; the display `label` is identity-invisible — a Phase 9 `UniverseSpecification`, a Phase 12 `RebalanceSchedule` of evaluation `as_of` instants, a `"<n>d"` forward horizon, an intercept flag defaulting on, and the two corpus pins — all folded into identity) drives `CrossSectionalRegressionEngine.estimate`, which re-verifies **both** corpora (fundamentals `dataset_version_id` **and** market `market_dataset_version_id`) and fails closed on any pin mismatch or non-unique normalizer (XS-1 — a changed corpus yields a different `crosssection_id`). At each scheduled `T` it rebuilds membership PIT as-of `T` (survivorship-free, Phase 9), reads the *K*-signal cross-section as **PIT-eligible-at-`T`** values via `panel_across(as_of=T)` (XS-3), and pairs each member with its realized **forward** return over `[T, T+h]` trading days through the Phase 11 PIT-gated adjusted view (the Phase 16 forward-return machinery reused verbatim); a member lacking any of the *K* PIT signals at `T` or a computable forward return is *excluded and recorded in coverage, never imputed* (XS-4). When the eligible-member count clears the degrees-of-freedom floor (`n_members >= K + include_intercept + 1`) it runs one exact-`Decimal` cross-sectional OLS of the forward returns on the *K* **raw** signals (plus an optional intercept) via the shared `quantforge._linalg` LDLᵀ solver with an **exact zero-pivot test** — a below-floor or singular date is a recorded `UNDEFINED` per-date block, never raised — then aggregates each coefficient's per-date series into a Fama–MacBeth premium (time-series mean, plain **population** standard error `popStd/√M`, t-statistic). A run yielding fewer than two valid dates fails closed (AG-5). The output is **ex-post, not PIT** (XS-2): `CrossSectionalRegression` is not a `Pit*` type and exposes no as-of accessor; `boundary_kind="pit"` documents only that the signal side was PIT-eligible. Every undefinable statistic (a singular/collinear per-date design, insufficient members, a zero-variance regressand, no or a single valid date, zero cross-date dispersion) is a first-class `UNDEFINED` cell carrying its reason — never a fabricated `0`, dropped factor, `NaN`/`Inf`, or divide-by-zero. The sealed `CrossSectionalRegression` is a `ResearchRecord` persisted write-once to the same Phase 8 sidecar (no new store), byte-identically round-tripping; `crosssection_id` folds the engine + formula + spec version, the full declared request, **both** corpus pins, and the `result_hash` over the computed answer (a per-date coefficient panel + premia block; coverage is audit metadata and is not folded). All arithmetic runs under the pinned `Decimal` context (prec 34, `ROUND_HALF_EVEN`); no float, wall-clock, or RNG enters any value or id. The one prior-phase edit is the behavior-preserving promotion of the exact-`Decimal` LDLᵀ solver into shared `quantforge._linalg` (AG-6; `attribution_engine_version_id` unchanged). Adds no runtime dependency, no database, and no new PIT resolution. See the [locked architecture](docs/phase18-cross-sectional-regression-locked.md). |
| **Factor-portfolio construction** | ✅ Exists (Phase 19) | A characteristic-sorted long/short factor-portfolio-construction layer — the first member of a new **portfolio-construction** capability class, a *constructive* sibling of the Phase 16 signal-diagnostics layer — sitting strictly *above* the Phase 9 universe, Phase 10 panel, and Phase 11 price layers and composing them only (it consumes **no** `BacktestResult` and produces none, P19-5). A declarative, content-addressed `FactorPortfolioSpecification` (a name, one signal `metric_key` + the explicit `MetricPeriod` it is read for, a Phase 9 `UniverseSpecification`, a Phase 12 `RebalanceSchedule` of evaluation `as_of` instants, a `"<n>d"` forward horizon, a quantile count `Q >= 2`, a leg-weighting scheme — v1 closed vocabulary `{"equal"}` — the annualization convention `risk_free_per_period` / `periods_per_year`, and the two corpus pins — all folded into identity) drives `FactorPortfolioEngine.construct`, which re-verifies **both** corpora (fundamentals `dataset_version_id` **and** market `market_dataset_version_id`) and fails closed on any pin mismatch or non-unique normalizer (P19-1 — a changed corpus yields a different `factor_portfolio_id`). At each scheduled `T` it rebuilds membership PIT as-of `T` (survivorship-free, Phase 9), reads the signal cross-section as **PIT-eligible-at-`T`** values via `panel_across(as_of=T)` (P19-3), and pairs each member with its realized **forward** return over `[T, T+h]` trading days through the Phase 11 PIT-gated adjusted view (the Phase 16 forward-return machinery reused verbatim); a member lacking the PIT signal at `T` or a computable forward return is *excluded and recorded in coverage, never imputed* (P19-4). It sorts the surviving members into `Q` quantile buckets by the PIT signal (the Phase 16 `quantile_buckets` rule reused verbatim), forms the **long** (top bucket) + **short** (bottom bucket) legs, equal-weights each leg, and computes the per-period factor return `f_T = mean(long) - mean(short)` (high-minus-low on the raw signal, dollar-neutral, gross); a period below the member floor (`n_members < 2·Q`) or with an empty long/short leg is a recorded `UNDEFINED` period, never raised, contributing no factor return. It then aggregates the `M` valid per-period returns into a summary — compounded cumulative `∏(1+f_T)-1`, mean, **population** volatility, annualized Sharpe `(mean-rf)/vol·√periods_per_year` (via `Decimal.sqrt`), the mean's t-statistic `mean/(popStd/√M)`, and hit rate. A run yielding fewer than two valid periods fails closed (`_MIN_VALID_PERIODS = 2`). The output is **ex-post, not PIT** (P19-2): `FactorPortfolio` is not a `Pit*` type and exposes no as-of accessor; `boundary_kind="pit"` documents only that the signal side was PIT-eligible. Every undefinable statistic (insufficient members, an empty long/short leg, no or a single valid period, zero return variance for the Sharpe/t-statistic) is a first-class `UNDEFINED` cell carrying its reason — never a fabricated `0`, `NaN`/`Inf`, or divide-by-zero. The sealed `FactorPortfolio` is a `ResearchRecord` persisted write-once to the same Phase 8 sidecar (no new store), byte-identically round-tripping; `factor_portfolio_id` folds the engine + formula + spec version, the full declared request, **both** corpus pins, and the `result_hash` over the computed answer (a per-period factor-return panel + summary block; per-period leg membership and coverage are audit metadata and are not folded). All arithmetic runs under the pinned `Decimal` context (prec 34, `ROUND_HALF_EVEN`); no float, wall-clock, or RNG enters any value or id. The only prior-phase edits are the additive `Workspace.factor_portfolio_engine` property and the `__init__.py` re-exports; no existing identity changes. Adds no runtime dependency, no database, and no new PIT resolution. See the [locked architecture](docs/phase19-factor-portfolio-locked.md). |
| **Factor risk model** | ✅ Exists (Phase 20) | A factor-risk-modelling layer strictly *above* Phase 19 — the first member of a new **risk-modelling** capability class, a pure-consumer sibling of the Phase 15 analytics / Phase 17 attribution layers (which likewise consume sealed artifacts rather than reading a raw corpus). A declarative, content-addressed `FactorRiskSpecification` (a name, an **ordered** tuple of `2..N_MAX = 16` sealed `FactorPortfolio` ids — order semantic, fixing the matrix row/column order and the `factor_1..factor_N` labels, no duplicate — and the annualization convention `periods_per_year`, folded into identity) drives `FactorRiskEngine.estimate`, which resolves each referenced factor from the shared Phase 8 sidecar via `store.read_as(id, FactorPortfolio.from_dict)` and re-verifies that the resolved record's `research_result_id` equals the requested id (fail closed on any missing/drifted reference); unlike Phase 17 there is no public content→hash recompute for a `FactorPortfolio`, so each factor's sealed `result_hash` is **folded into the model's identity**, making the id transitively sensitive to any change in any referenced factor (FR-1). It enforces commensurability — every factor must share one exact `schedule_id` **and** one `factor_portfolio_engine_version_id`, else fail closed (FR-3; a corpus-pin difference is **surfaced** as `pin_mismatch`, never raised or reconciled). It complete-case aligns the factors' KNOWN `(as_of, factor_return)` series on the intersection of dates where **every** factor is KNOWN (ascending; never filled or interpolated), requiring at least `_MIN_PERIODS = 2` common dates or it fails closed (FR-4). Over that window it estimates, under the pinned `Decimal` context, the per-factor mean and **population** volatility (`√((1/M)Σ(f-mean)²)` via `Decimal.sqrt`), the `N x N` population covariance matrix `cov(i,j) = (1/M)Σ(f_i-mean_i)(f_j-mean_j)`, and the companion correlation matrix `corr(i,j) = cov(i,j)/(vol_i·vol_j)` — per-period and annualized (`vol·√ppy`, `cov·ppy`), stored as the **upper triangle** only (`i <= j`; the lower triangle is implied by symmetry). A zero-variance factor's correlation cell (its `0/0` diagonal included) is a first-class `UNDEFINED` `ZERO_VARIANCE` value — never a divide-by-zero, fabricated `0`, `NaN`/`Inf` (FR-4); the means, volatilities (KNOWN even at exactly `0`), and every covariance cell stay KNOWN. The output is **ex-post, not PIT** (FR-2): `FactorRiskModel` is not a `Pit*` type and exposes no as-of accessor; `boundary_kind="pit"` documents only that the underlying factor portfolios were PIT walks, and it is **not a `BacktestResult`** (FR-5). The sealed `FactorRiskModel` is a `ResearchRecord` persisted write-once to the same Phase 8 sidecar (no new store), byte-identically round-tripping; `factor_risk_id` folds the engine + formula + spec version, the declared request, the **ordered** factor `result_hash`es, and the `result_hash` over the computed answer (the per-factor moment block + upper-triangle covariance + upper-triangle correlation cells; the coverage summary is audit metadata and is not folded). All arithmetic runs under the pinned `Decimal` context (prec 34, `ROUND_HALF_EVEN`); no float, wall-clock, or RNG enters any value or id. The only prior-phase edits are the additive `Workspace.factor_risk_engine` property and the `__init__.py` re-exports; no existing identity changes. Adds no runtime dependency, no database, no new data source, and no new PIT resolution. See the [locked architecture](docs/phase20-factor-risk-model-locked.md). |
| **Portfolio optimization** | ✅ Exists (Phase 21) | A factor-risk-aware portfolio-optimization layer strictly *above* Phase 20 — the **first optimization layer** in the project and the first member of a new **optimization** capability class, structurally the pure-consumer sibling of Phase 20 (as Phase 20 consumes sealed `FactorPortfolio`s, Phase 21 consumes one sealed `FactorRiskModel`). A declarative, content-addressed `PortfolioOptimizationSpecification` (a name, exactly one sealed `factor_risk_id`, the objective `minimum_variance` — the sole v1 vocabulary — and the constraint flag `fully_invested` which must be identically `True`, all folded into identity) drives `PortfolioOptimizationEngine.optimize`, which resolves the referenced model from the shared Phase 8 sidecar via `store.read_as(id, FactorRiskModel.from_dict)` and re-verifies that the resolved record's `research_result_id` equals the requested id and that the payload decodes as a `FactorRiskModel` (fail closed on any missing / drifted / wrong-type reference); the model's sealed `result_hash` is **folded into the optimization's identity**, making the id transitively sensitive to any change in the risk model, its factors, or its corpus (PO-1). It re-checks the inherited factor-count bound `_MIN_FACTORS = 2 <= N <= N_MAX = 16` (fail closed), then reconstructs the full symmetric `N x N` factor covariance `Σ` from the model's sealed **upper-triangle** cells — re-verified fail-closed (every `i <= j` position present exactly once, in range, KNOWN, a finite decimal string) — consuming it as-is, never recomputed, shrunk, or regularized (PO-3), and never fabricating an expected-return / risk-free / benchmark input. Under the pinned `Decimal` context it solves the **fully-invested global minimum-variance** problem `min wᵀΣw s.t. 1ᵀw = 1` in **closed form** via the existing exact-`Decimal` `quantforge._linalg` LDLᵀ primitives (`ldl` / `ldl_solve`, unchanged): factorize `Σ = LDLᵀ`, solve `Σx = 1`, `s = 1ᵀΣ⁻¹1`, `wᵢ = xᵢ/s`, achieved per-period variance `wᵀΣw` (an inline quadratic form), volatility `√variance`. A GMV weight may be negative (an honest long/short across factors); no non-negativity applies in v1. A non-positive-definite `Σ` — the exact LDLᵀ **zero-pivot test**, no float tolerance (plus defensive non-positive-`s` / negative-quadratic-form guards) — is a first-class `UNDEFINED` `SINGULAR_COVARIANCE` result with every weight, variance, and volatility UNDEFINED together, never a divide-by-zero, pseudo-inverse, dropped factor, or repaired matrix (PO-4). The output is **ex-post, not PIT** (PO-2): `PortfolioOptimization` is not a `Pit*` type and exposes no as-of accessor; `boundary_kind="pit"` documents only that the underlying factor portfolios were PIT walks, and it is **not a `BacktestResult`** and performs no execution / holdings / cash / cost accounting (PO-5). A distinct-corpus-pin condition carried from the risk model is surfaced as `pin_mismatch`, never reconciled. The sealed `PortfolioOptimization` is a `ResearchRecord` persisted write-once to the same Phase 8 sidecar (no new store), byte-identically round-tripping; `optimization_id` folds the engine + solve + spec version, the declared request + canonical constraint spec, the covariance basis (`per_period`), the referenced `factor_risk_id` + its `result_hash`, and the `result_hash` over the computed answer (the ordered status / per-factor-weight / variance / volatility cells). All arithmetic runs under the pinned `Decimal` context (prec 34, `ROUND_HALF_EVEN`); no float, iteration, wall-clock, or RNG enters any value or id, and `_linalg` is unchanged. The only prior-phase edits are the additive `Workspace.optimization_engine` property and the `__init__.py` re-exports; no existing identity changes. Adds no runtime dependency, no database, no new data source, and no new PIT resolution. See the [locked architecture](docs/phase21-portfolio-optimization-locked.md). |
| **Walk-forward evaluation** | ✅ Exists (Phase 22) | A walk-forward out-of-sample-evaluation layer strictly *above* Phase 21 — the **first genuine consumer** of the Phase 21 optimizer (turning its terminal-leaf `PortfolioOptimization` into an input) and the project's first **train-before-test** temporal discipline. A declarative, content-addressed `WalkForwardEvaluationSpecification` (a name, exactly one sealed `optimization_id` recipe, and a `TrainingPolicy` — `window` `expanding`\|`rolling`, `min_train_periods >= 2`, `test_periods >= 1`, `rolling_length` present iff rolling — all folded into identity) drives `WalkForwardEvaluationEngine.evaluate`, which resolves the recipe from the shared Phase 8 sidecar via `store.read_as(id, PortfolioOptimization.from_dict)` and re-verifies it (id match, `status = OPTIMAL`, objective `minimum_variance`, constraint exactly `{"fully_invested": True}` — a future constrained recipe can never be silently walked, WF-5), then resolves and re-verifies the transitively-referenced `FactorRiskModel` (with a `result_hash` pin match) and every `FactorPortfolio` (WF-1), enforcing one shared inherited `risk_free_per_period` (a disagreement fails closed) and the factor-count bound `2 <= N <= N_MAX = 16`. It complete-case aligns the factors' KNOWN `(as_of, factor_return)` series on the intersection of dates where **every** factor is KNOWN (ascending; never filled or interpolated, WF-6), and partitions that axis into ordered `train → test` windows on rebalance cuts `c_k = min_train_periods + k·test_periods` (expanding train `[0, c_k)`, rolling train `[max(0, c_k − rolling_length), c_k)`, test `[c_k, min(c_k + test_periods, M))`) with a strict no-look-ahead split `train_end == test_start` (WF-2). Per window, under the pinned `Decimal` context, it **re-estimates** the `N x N` per-period covariance over the training span (composing the Phase 20 `estimate_moments`), **re-solves** the fully-invested GMV weights over it (composing the Phase 21 `solve_min_variance`), and **realizes** those weights held constant against the strictly-subsequent test returns `r_t = Σ_i w_{k,i}·f_{i,t}`; it seals per-window `predicted_variance` (in-sample `wᵀΣw`) and `realized_variance` (population variance of that window's OOS returns). A non-positive-definite training covariance (the exact Phase 21 LDLᵀ zero-pivot test) is a first-class `UNDEFINED` `SINGULAR_TRAINING_COVARIANCE` window carrying no weights and no OOS returns — never a divide-by-zero, pseudo-inverse, dropped, filled, or regularized window (WF-4); the evaluation still seals. It chains the REALIZED windows' OOS returns and summarizes them by composing the Phase 19 `series_summary` (compounded cumulative, mean, population volatility, annualized Sharpe, t-statistic, hit rate), plus the aggregate realized OOS variance. Fewer than `MIN_VALID_WINDOWS = 2` candidate or REALIZED windows fails closed — no defensible OOS summary (WF-4). Phase 22 introduces **no new numerical formula**: it composes three pinned pure methods (Phase 19/20/21), whose versions are folded into the engine identity alongside the decimal context and its own `walkforward-method/1` (WF-5), so a bump in any composed method yields a new engine id. The output is **ex-post — not a PIT value and not a `BacktestResult`** (WF-3): `WalkForwardEvaluation` is not a `Pit*` type and exposes no as-of accessor; `boundary_kind="pit"` documents only that the underlying factor portfolios were PIT walks; carried corpus pins are surfaced as `pin_mismatch`, never reconciled. The sealed `WalkForwardEvaluation` is a `ResearchRecord` persisted write-once to the same Phase 8 sidecar (no new store), byte-identically round-tripping; `walk_forward_id` folds the engine + composed-method + decimal-context version, the declared request + canonical training policy, the inherited `schedule_id`, the referenced `optimization_id` + its `result_hash` (transitive pin), and the `result_hash` over the computed walk (per-window blocks in schedule order, then the chained OOS series, summary, and aggregate realized variance; per-window `oos_returns` are audit metadata and are not folded). All arithmetic runs under the pinned `Decimal` context (prec 34, `ROUND_HALF_EVEN`); no float, iteration, wall-clock, or RNG enters any value or id, and `_linalg` is unchanged. The only prior-phase edits are the additive `Workspace.walk_forward_engine` property and the `__init__.py` re-exports; no existing identity changes. Adds no runtime dependency, no database, no new data source, and no new PIT resolution. See the [locked architecture](docs/phase22-walk-forward-evaluation-locked.md). |
| **Research-campaign evaluation** | ✅ Exists (Phase 23) | An out-of-sample research-campaign-evaluation layer strictly *above* Phase 22 — the **first genuine consumer** of the Phase 22 terminal-leaf `WalkForwardEvaluation` and the project's first **selection-bias / meta-analysis** layer. A declarative, content-addressed `ResearchCampaignSpecification` (a name, an **ordered** tuple of `2..N_MAX = 64` sealed `WalkForwardEvaluation` ids — the *trials* of one research campaign, order semantic and never sorted, fixing the `trial_1..trial_N` labels, the selection index, and the search size — and a per-period benchmark Sharpe `SR*` defaulting to `"0"`, all folded into identity) drives `ResearchCampaignEngine.evaluate`, which resolves each trial from the shared Phase 8 sidecar via `store.read_as(id, WalkForwardEvaluation.from_dict)` and re-verifies that the resolved record's `research_result_id` equals the requested id and its roll-up `status` is `REALIZED` (fail closed on any missing / drifted / non-`REALIZED` reference); each trial's sealed `result_hash` is **folded into the campaign's identity** (in request order), making the id transitively sensitive to any change in any referenced trial and, through it, the optimization → risk model → factors → corpora (CE-1). It enforces commensurability — every trial must share one exact `schedule_id` **and** one `factor_portfolio_engine_version_id`, else fail closed (CE-3; a corpus-pin difference is **surfaced** as `pin_mismatch`, never raised or reconciled). Per trial, under the pinned `Decimal` context, it re-derives from the sealed `oos_returns` (with the trial's inherited `risk_free_per_period`, **never** the sealed annualized Sharpe) the per-period excess-return **Sharpe**, **skew**, and **non-excess kurtosis**, and the **Probabilistic Sharpe Ratio** `PSR(SR*) = Φ((SR−SR*)·√(n−1)/√(1−γ₃·SR+((γ₄−1)/4)·SR²))`. Across trials it takes the search size `N` as the count of **all** submitted trials — valid *and* `UNDEFINED` (CE-2) — the **population** variance `V` of the valid trials' Sharpe ratios, the **expected-maximum Sharpe under the null** `SR₀ = √V·[(1−γ)·Z⁻¹(1−1/N)+γ·Z⁻¹(1−1/(N·e))]` (γ = Euler–Mascheroni), and the headline **Deflated Sharpe Ratio** `DSR = PSR(SR₀)` of the selected (maximum-Sharpe, ties→lowest request index) trial. It introduces exactly **one** new numerical primitive — a deterministic exact-`Decimal` standard-normal `Φ` (`½(1+erf(x/√2))` via an all-positive-term `erf` series) / `Z⁻¹` (a fixed-iteration monotone bisection), phase-local in `campaign/normal.py`, **not** an `_linalg` change (CE-5) — and composes a self-contained exact-`Decimal` moment computation (the approved fallback, as `analytics` moments are not cleanly importable). Every undefinable statistic — a trial with fewer than two OOS periods (`INSUFFICIENT_OOS_PERIODS`), zero OOS population variance (`ZERO_OOS_VARIANCE`), or a non-positive PSR estimator variance (`DEGENERATE_SHARPE_ESTIMATOR`); a campaign with fewer than `MIN_VALID_TRIALS = 2` valid trials (`INSUFFICIENT_VALID_TRIALS`) — is a first-class `UNDEFINED` cell carrying its reason, excluded from selection and from `V`, never a fabricated `0`, `NaN`/`Inf`, or divide-by-zero (CE-4); the record still seals. The output is **ex-post — not a PIT value and not a `BacktestResult`** (CE-6): `ResearchCampaignEvaluation` is not a `Pit*` type and exposes no as-of accessor; `boundary_kind="pit"` documents only that the underlying trials were PIT walks. The sealed `ResearchCampaignEvaluation` is a `ResearchRecord` persisted write-once to the same Phase 8 sidecar (no new store), byte-identically round-tripping; `campaign_id` folds the engine + method + normal-primitive + decimal-context version, the declared request (name, spec version, the **ordered** trial ids, the canonical benchmark Sharpe), the **ordered** trial `result_hash`es (transitive pin), and the `result_hash` over the computed answer (the per-trial statistic cells in request order, then the campaign-summary cell). All arithmetic runs under the pinned `Decimal` context (prec 34, `ROUND_HALF_EVEN`); no float, wall-clock, or RNG enters any value or id, and `_linalg` is unchanged. The only prior-phase edits are the additive `Workspace.campaign_engine` property and the `__init__.py` re-exports; no existing identity changes. Adds no runtime dependency, no database, no new data source, and no new PIT resolution. See the [locked architecture](docs/phase23-research-campaign-evaluation-locked.md). |
| **Strategy comparison** | ✅ Exists (Phase 24) | A pairwise out-of-sample strategy-comparison layer strictly *above* Phase 22 — the project's first **relative-testing** layer and the first genuine consumer of *multiple* `WalkForwardEvaluation`s together (Phase 23 aggregates them into one campaign statistic; Phase 24 tests them against each other). A declarative, content-addressed `StrategyComparisonSpecification` (a name, an **ordered** tuple of `2..N_MAX = 32` sealed `WalkForwardEvaluation` ids — the competing *strategies*, order semantic and never sorted, fixing the `strategy_1..strategy_N` labels and the matrix row/column order — all folded into identity) drives `StrategyComparisonEngine.compare`, which resolves each strategy from the shared Phase 8 sidecar via `store.read_as(id, WalkForwardEvaluation.from_dict)` and re-verifies that the resolved record's `research_result_id` equals the requested id and its roll-up `status` is `REALIZED` (fail closed on any missing / drifted / non-`REALIZED` reference); each strategy's sealed `result_hash` is **folded into the comparison's identity** in request order, making the id transitively sensitive to any change in any referenced strategy and, through it, the optimization → risk model → factors → corpora (SC-1). It enforces commensurability — every strategy must share one exact `schedule_id`, `factor_portfolio_engine_version_id`, `periods_per_year`, **and** `risk_free_per_period`, else fail closed (SC-2; a corpus-pin difference is **surfaced** as `pin_mismatch`, never raised or reconciled). For each strategy it **reconstructs** the realized OOS per-period return series by re-resolving the transitive `optimization_ref → PortfolioOptimization.risk_model_ref → FactorRiskModel factor refs → FactorPortfolio.per_period` chain and recomputing the deterministic complete-case **calendar-date** axis (identical to Phase 22's `_known_returns` / `_common_dates` logic), mapping each REALIZED window to `(as_of → return)`. For each **upper-triangle** `(i<j)` pair it aligns the two reconstructed series by **calendar-date intersection** — the disclosed deviation from the proposal's axis-index SC-3 alignment — and, over the aligned per-period differences `d_t = r_t^i − r_t^j` (`T = overlap`), computes under the pinned `Decimal` context the mean difference `d̄ = Σd/T`, the **population**-variance standard error `stderr = √((Σ(d−d̄)²/T)/T)`, the paired `t = d̄/stderr`, a two-sided `p = 2·(1 − Φ(|t|))` clamped to `[0,1]` via the **reused** exact-`Decimal` standard-normal `Φ`, and a descriptive `sharpe_diff` = difference of the two strategies' sealed **annualized** OOS Sharpe strings (a passthrough, not recomputed). The `Φ` / `Z⁻¹` primitive introduced in Phase 23 is **extracted verbatim** to a shared `quantforge/_stats/normal.py` (a byte-identical refactor that preserves every Phase 23 id; `campaign/normal.py` re-exports it) — no new numerical primitive, no `_linalg` change (SC-6). Too little overlap (`< MIN_OVERLAP_PERIODS = 2` → `INSUFFICIENT_OVERLAP`), a zero-variance paired difference (`ZERO_DIFFERENCE_VARIANCE`; `mean_diff` stays KNOWN, `t`/`p` UNDEFINED), or an undefined leg Sharpe (`UNDEFINED_STRATEGY_SHARPE` — the disclosed third reason beyond the proposal's closed set) is a first-class `UNDEFINED` cell carrying its reason, never a divide-by-zero, fabricated `0`, or `NaN`/`Inf` (SC-4). Only the `i<j` triangle is stored; the `(j,i)` view is an exact sign-flip of `mean_diff` / `t_stat` / `sharpe_diff` via `Decimal.copy_negate` (context-free, exact — never unary minus, which rounds to the ambient context; a negated zero is re-canonicalized to positive zero), preserving `p_value` / `stderr_diff` / `overlap_periods` (SC-8). The layer is **measurement-only** — it applies no multiple-comparison correction (a deliberate ex-post scope, deferred to a future family-wise layer). The output is **ex-post — not a PIT value and not a `BacktestResult`** (SC-5): `StrategyComparison` is not a `Pit*` type and exposes no as-of accessor; `boundary_kind="pit"` documents only that the underlying strategies were PIT walks. The sealed `StrategyComparison` is a `ResearchRecord` persisted write-once to the same Phase 8 sidecar (no new store), byte-identically round-tripping; `strategy_comparison_id` folds the engine + method + normal-primitive + decimal-context version, the declared request (name, spec version, the **ordered** strategy ids), the **ordered** strategy `result_hash`es (transitive pin), and the `result_hash` over the computed answer (the per-strategy trial summaries in request order, then the upper-triangle comparison cells; coverage is audit metadata and is not folded). All arithmetic runs under the pinned `Decimal` context (prec 34, `ROUND_HALF_EVEN`); no float, wall-clock, or RNG enters any value or id, and `_linalg` is unchanged. The only prior-phase edits are the additive `Workspace.comparison_engine` property, the `__init__.py` re-exports, and the byte-identical `Φ` extraction to `_stats/normal.py`; no existing identity changes. Adds no runtime dependency, no database, no new data source, and no new PIT resolution. See the [locked architecture](docs/phase24-strategy-comparison-locked.md). |
| **Repository & tooling foundation** | ✅ Exists | Packaging (src layout, `pyproject.toml`, `uv.lock`), tests, lint/format, type checking, pre-commit, and docs. |

## Design intent

- The **immutable raw data** store is the system of record. Everything
  downstream is a deterministic function of it.
- **Provenance** links every derived value back to the raw data and the exact
  transformation that produced it.
- The **point-in-time data layer** is what makes research free of look-ahead
  bias: queries are answered as of a specified date using only information that
  was known at that date.
- **Reproducibility** is the end-to-end property that ties the pipeline
  together: the same inputs and code always produce the same outputs.

## Technology notes

- **Python 3.11+**, managed with **uv** (`pyproject.toml` + `uv.lock`).
- **src layout** for the package.
- Runtime dependencies are added only when a component that needs them is
  implemented. There are none yet.
- DuckDB is a likely candidate for local analytical storage, but nothing is
  committed to yet and no database code exists.

## Engineering Principles

These principles are non-negotiable and guide every design decision.

1. **Correctness over convenience.** When the two conflict, correctness wins,
   even if it is slower to build or use.
2. **Immutable raw data.** Raw source data is append-only and never modified
   after capture. It is the system of record.
3. **Provenance.** Every derived value must be traceable to the raw data and the
   transformation that produced it.
4. **Point-in-time integrity.** Data is served as it was known at a given date.
   Look-ahead bias is treated as a correctness bug.
5. **Deterministic transformations.** The same inputs and code must always
   produce the same outputs. No hidden state, no reliance on wall-clock time or
   nondeterministic ordering.
6. **Reproducibility.** Analyses and backtests can be re-run to produce
   identical results, end to end.
7. **Tests for critical behavior.** Anything affecting correctness or
   point-in-time integrity must be covered by tests.
8. **No fabricated financial data.** The project never invents financial data,
   and never ships example data that could be mistaken for real market data.
9. **No secrets in source control.** Credentials and secrets stay out of the
   repository; local secrets live in git-ignored `.env` files.
10. **Minimal dependencies.** A dependency is added only when genuinely needed,
    and its inclusion is justified.
